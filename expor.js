let num = [2,0,2,1,1,0];
let ind = []
const ordern = num.reduce(function(index, element) {
  for (let i = 0; i < num.length; i++) {
    const element = num[i];
    if () {
      
    }
    
  }
})

console.log("Respuesta", ind);